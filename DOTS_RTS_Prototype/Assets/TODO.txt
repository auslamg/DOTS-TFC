===================================================================================
//TODO
===================================================================================

Optimize render resolution
- LOOK INTO: https://www.youtube.com/shorts/eYke6tpAdws
- LOOK INTO: Touch up RP assets > lights > Shadow Resolution & Shadow Atlas Resolution for fx optimization

- LOOK INTO: Leak Detected : Persistent allocates 8 individual allocations. To find out more please enable 'Preferences > Jobs > Leak Detection Level > Enabled With Stack Trace' and reproduce the leak again.

Repository
- Track "Scripts\Util"
- Check ScriptableObject persistency

Document
- DOCUMENT: Camera pinch bug: releasing both touches in quick succesion counts as a super-fast drag
- ANIMATION ADDITION PROCESS:
    UnitAnimationsAuthoring definition
    AnimationDataSO AnimationType definition
    Editor ScriptableObject instance
    Set SO Instance fields
    Add SO to AnimationDataListSO
    Set UnitAnimations component fields

    FOR PREEXISTING ANIMATION TYPES:
        Create ScriptableObjects per animation
            KEY
            TYPE
            PLAYFULL
            MESHARRAY
            FRAME FREQUENCY
        Add keys to UnitAnimations managed component
        Add ScriptableObjects to AnimationDataRegistrySO list

- UNMANAGED PHYSICS QUERIES PROCESS
    Get PhysicsWorldSingleton for CollisionWorld
    Make CollisionFilter with appropiate layers
    Cache NativeList<DistanceHit> with allocated memory
    Clear list before running
    Call collisionWorld.OverlapX() passing these:
        - Position
        - Radius
        - ref NativeList<DistanceHit>
        - CollisionFilter

- BUILDING ADDITION PROCESS: 
    FOR PREEXISTING BUILDING TYPES:
        Create prefab
        Change model
        Change collider proportions
        Create BuildingDataSO
        Add BuildingDataSO to registry
        Add key to BuildingDataSOHolder component

- PATHFINDING REQUEST PROCESS:
    Move order handled from UnitSelectionManager (might refactor).
    PathRequestJob handles pathing method (straight if available, navigation if required).
    GridSystem calculates FlowFields for all PathRequests that require it.
    Follow flow field while the path to target is not clear.
    Whenever it is clear, disable flowFieldFollower and just set unitMover target position to the final destination.



Aesthetic/UX addons
- FEATURE/FIX: Right click on friendly unit = move (click layer priorities)
- FEATURE: Randomized animation delay (time or frame based) for visual noise
- FEATURE: Parabollic path arrow trajectory
- FEATURE: UX particle feedback (spawn particles on events)
    - OnSpawnUnit: smoke cloud
    - OnTargetSelect: cross-swords / crosshair
    - OnKillUnit: skull / crossbones
- UI: Change selected gizmo for buildings
- UI: Add a SelectedUI to display selected elements
- FIX: Camera pinch bug: releasing both touches in quick succesion counts as a super-fast drag
- FEATURE: Setting trainer rally point to an unreachable/ unwalkable position shouldn't be allowed
- REFACTOR UI: Make Unit roster train buttons and queue buttons their own subclasses to reduce BuildingTrainerUI file size and increase clarity.


Gameplay features
- FEATURE: ActionQueue: Queue multiple actions in FCFS while holding shift.
    >> IDEA: Make it (A) a struct with enum "ActionType" and an "ActionDecoder" system, or (B) an interface "IQueuableAction" with different fields
    - FEATURE: When manually attacking a unit, add surrounding units to queue as attack actions.
- FIX: Spawner spawned entites spawn inside the building and cannot navigate outside. Add an external spawn point.
- FIX: Units straight pathing in large formations to positions close to walls will make units go through the walls themselves. This isn't ideal since it defeats the purpose of walls.
- FIX: Melee attack untis should face target

- FIXED: FIX: Cannot build more added buildings ??? Add prefab registry entry

- SAVE GAME SYSTEM IDEA:
    ISaveableComponent interface for components that 100% require persistence
    EntityID IComponentData to identify entities post-allocation 
    Convert entity references from runtime id's to persisted id's
    Serialize to binary for performance and filesize and Safety

    INVESTIGATE
    Serialize by chunk (???)
    Save asynchronously (???)
    Compress save files - LZ4 (???)

    OR 

- JUST USE SerializeUtility ( HIGHLY EXPERIMENTAL )



Performance/data features
- FEATURE: Texture Atlas pre-runtime calculation
- IDEA: Implement Dictionary style Map for AnimationDataRegistry (reduce runtime magnitude from O(log n) to O(1) at the expense of a slower initial bake)


AI features
- FEATURE: FSM bot enemy? Check for DOTS compatibility
- FEATURE: Non-seeking, calculated projectiles (Catapult style?)

- GDD: Full targetting behaviour
- GDD: Targets dont swap if they dont pass the threshold
- FEATURE/FIX: After losing their target, RandomWalk entities should check for new targets before getting a random position
- FEATURE/FIX: Make two options more moveAttack rallypoint and normal only move rallypoint for trainers

Refactors
- REFACTOR: Rename entities at runtime for identification ease (state.EntityManager.SetName)
- REFACTOR: Change ALL Instantiate references to EntityCommandBuffer pattern
- REFACTOR: Implement hard requirements for dependencies in authoring files during baking process for unmanaged components (Investigate)

- REFACTOR: Implement SO-Oriented attributes (health, damage, etc.) and retrieve from those during entity baking
- REFACTOR/OPTIMIZE: Pathfinding time complexity / scalability.
    - IDEA: Add Leaders-Followers optimization to Pathing system: FlowField request should save input coordinate (pre-formation calculation) and group it with other entities with the same coordinate. Select a leader and calculate the FlowField once, then through straight path requests relocate positions to calculated formation.
    - IDEA: Make buildings only constructible in a certain radius around a Fort building. Therefore, navigation is only necesary in those radiuses and smaller grids are only added around those. General "baked-in" map navigation would not need to be recalculated during runtime.
    - IDEA: Make all ordered units navigate to a single center click position and save a secondary post-formation position that should only be navigated to when reachable in a straight line. If not reachable, move as far as possible in the relative direction to the order position.
    - IDEA: Instead of overlapping every single cell for obstruction detection, make ONE single overlap and then grow the overlaps in flood fills around the detected models
    - IDEA: Instead of overlapping every single cell for obstruction detection, divide the grid in chunks and realize a dichotomic occupied cell physics query (where 64x64 is checked, if true, 4 * 32x32, until size is 1x1)
- REFACTOR/FIX: Validate non-nullity in all baker key fields
- REFACTOR/IDEA: Use scriptable object references instead of strings for managed KeyIdentification in authoring classes to implement rename refactor automatic name compatibility.


===================================================================================
//FIXED
===================================================================================

FIXED: Refactor unit turn speed source
FIXED: ZOMBIE CANT FIND TARGET
FIXED: Fix unit selection rect scaling with screen size
FIXED: Could not find singleton oftype EntitiesReference


===================================================================================
//DONE
===================================================================================

//REVIEW:
 DOCUMENT: Make all summary comments visible in SerializeFields + Tooltips (Copilot might do this real good)

DONE: DOCUMENT: (Animation baking extensively)

DONE: REFACTOR: ShootLightSpawner to OnShootSpawner (parametrize prefab)
DONE: REFACTOR: Rename MoveOverride to ManualMove
DONE: REFACTOR: Rename TargetOverride to ManualTarget
DONE: REFACTOR: Remove "Authoring" from all baker signatures
DONE: REFACTOR: Animation system into dictionary values rather than enums (see documentation)
DONE: REFACTOR: Change unit selector method for single unit for spherecast OR increase selection hitboxes
DONE: REFACTOR: Remove commented obsolete code from the animation baking authoring file
DONE: REFACTOR: Parametrize EnemySpawner and rename to Spawner (change SO)
DONE: RENAME: Barracks scripts to Trainer
DONE: REFACTOR: >= consistency in timer code blocks
DONE: REFACTOR: "Frequency" variables in struct timers should be "peiod" or "interval"
DONE: REFACTOR: Replace component/system timer fields by Timer common struct {float max, float curr}.


DONE: REFACTOR/OPTIMIZE: Add Leaders-Followers optimization to Pathing system: FlowField request should save input coordinate (pre-formation calculation) and group it with other entities with the same coordinate. Select a leader and calculate the FlowField once, then through straight path requests relocate positions to calculated formation.
    Athough not exactly executed that way, the idea that one single flowfield is calculated regardless of army size has been efectively implemented already.

- DONE: UI: Add a UIStateMachine / UI manager to switch out different UIs
- DONE: UI: Building ghosts should display valid placement in real time by changing colors



- Attack detection QOL ngd
- Deselect all button DONE
- OnSelect > ActionMode DONE
- Selection cast priority ngd
- Attack action collider instead of ray DONE
- ManualMove targetting bugs 

- Adjust resources and costs DONE
- Adjsut waves and duration and win condition DONE

- Check for target conflicts ~CHECK
- Check out of map validation ~CHECK

- Load from MainMenu
- Mobile build UI